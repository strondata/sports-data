# sports-data

Um framework ETL desenvolvido em Python para abstrair a recolha, limpeza e armazenamento de dados de performance desportiva.

## Como correr
Instale com poetry:
```bash
poetry install
```

Corra o cli:
```bash
poetry run sports-data run --url <url>
```
